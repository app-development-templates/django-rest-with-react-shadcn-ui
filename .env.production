# Production Environment Configuration for Dokploy

# Database Configuration
DB_HOST=db
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres

# Application Configuration
DEBUG=0
SECRET_KEY=change-me-in-production-please-use-a-secure-random-string
ALLOWED_HOSTS=*

# CORS Configuration (adjust for your domain)
CORS_ALLOWED_ORIGINS=*

# API Configuration (Dokploy will set this automatically)
VITE_API_URL=http://backend:8000

# Note: In production, Dokploy will:
# 1. Assign random external ports to avoid conflicts
# 2. Handle SSL/TLS termination
# 3. Provide proper domain names
# 4. Manage environment variables through the UI